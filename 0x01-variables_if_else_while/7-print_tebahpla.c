#include <stdio.h>

/**
 * main - print lowercase reverse
 *
 * Return - Always 0 (success)
 */
int main(void)
{
	int ch;

	for (ch = 26; ch < 0; ch--)
	{
		putchar(ch);
	}
	putchar('\n');
	return 0;
}
