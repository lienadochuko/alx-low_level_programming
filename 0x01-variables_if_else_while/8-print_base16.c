#include <stdio.h>

/**
 * main - prints base16 numbers.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 41; i < 51; i++)
	{
		putchar(i);
	}

	for (i = 89; i < 95; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
