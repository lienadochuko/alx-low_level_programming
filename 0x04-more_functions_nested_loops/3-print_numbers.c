#include "mian.h"

/**
 * print_numbers - Prints the numbers followed by new line.
 * @c: contains numbers.
 * Return: numbers.
 */
void print_numbers(void)
{
	if (c >= 47 && c <= 57)
	{
		_putchar(c);
		_putchar('\n');
	}
}
