#include "mian.h"

/**
 * print_numbers - Prints the numbers followed by new line.
 * @c: contains numbers.
 * Return: numbers.
 */
void print_numbers(void)
{
	int c;

	for (ch = 48; ch < 58; c++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
